# Superseded manuscript drafting notes

The earlier narrative scaffold in this file was superseded by the audited
manuscript on 2026-08-23. It is retained at this path only so references to the
drafting file do not silently break. It is not an evidentiary source and must
not be used to recover numerical claims.

Authoritative current sources are:

- `paper/main.tex` for the scientific narrative;
- `paper/claim_ledger.csv` for claim-level evidence and inclusion decisions;
- `paper/data/*.json` and `paper/data/*.csv` for machine-readable results;
- `paper/protocol/` for prospectively specified local protocols; and
- `paper/supplement/PROVENANCE_AUDIT.md` for conflicts, missing artifacts, and
  restricted-release boundaries.

The red-team review materially narrowed the representation claim. The repaired
coordinate supplies a missing option-to-source-card type/ID binding, but the
normalized hand-index coordinate was already present. Consequently, the exact
within-state blind-input collision count in the retained rows is zero; an
earlier corpus-wide signature grouping was not an action-collision statistic
and is rejected. The training rows are winner/top-episode selected recorded
actions, not an independently validated sample of expert-optimal decisions.

The later coupling audit also invalidated the planned seven-opponent four-cell
analysis.  Repeated C1 outcomes differed on 210 of 2,800 seed-condition units
because the seeded engine schedule did not control wall-clock/process-local
opponent search.  The current manuscript therefore treats the primary result as
a one-execution schedule comparison and makes no confirmatory four-cell
mechanistic claim.

All prose and values in the manuscript source are generated from or checked
against the current audited sources above.  Compilation and rendered-page
inspection remain required before submission.
