# Equation audit — FINAL (machine-finalization refresh)

Date: 2026-08-25. Status: **all machine checks PASS today** (`verify_statistics.py` PASS;
`independent_statistics_audit.py --check` PASS with 0 conflicts; `tests/test_equations.py`
PASS inside the release builder run). Human comprehension signoff remains
PENDING (HUMAN_CLOSEOUT_FORM.md §12.1). Authoritative background:
`EQUATION_AUDIT.md` (V1) and `EQUATION_AUDIT_V2.md` (closeout); nothing here weakens them.

## EQ01 — Trace-disagreement proportion (main.tex `eq:trace-disagreement`)

- Symbols: $N$ seed-condition clusters; $\mathcal{R}$ complete prescribed profile set;
  $H_{ir}=(D_{ir},B_{ir})$ ordered SHA-256 digest + byte count for cluster $i$, profile $r$.
- Units: none (proportion of seed conditions).
- Analysis unit: seed-condition cluster containing all execution profiles.
- Assumptions: canonical serialization; correct digest implementation; all claim-relevant fields declared.
- Toy calculation: two of five clusters disagree (one digest-agrees/byte-differs) → $\widehat q=2/5=0.40$.
- Sign/index direction: indicator fires when ≥2 required pairs differ; higher = worse repeatability.
- Source implementation: release verifier cluster logic; fail-closed on missing required profile.
- Independent test: `tests/test_equations.py` + `recalculate_equation_examples.py` (PASS today; byte-count-only detection true; missing-profile fail-closed true).

## EQ02 — Schedule-indexed paired difference (`eq:paired-difference`)

- Symbols: $Y^{(I)},Y^{(C)}\in\{0,1\}$; $d_i=Y_i^{(I)}-Y_i^{(C)}$.
- Units: none; presented as percentage-point win-rate difference at battery level.
- Analysis unit: schedule-indexed paired unit (frozen opponent/order/seat/boundary seed).
- Assumptions: pairing fields frozen by protocol; no distribution assumed.
- Toy calculation: intervention wins, control does not → $d=1-0=1$.
- Sign/index direction: positive favors the intervention arm (I minus C).
- Source implementation: analyzer paired-difference aggregation over factorial units.
- Independent test: direction + value test in `tests/test_equations.py` (PASS today: paired_difference=1).

## EQ03 — Stratified empirical reweighting (`eq:paired-reweighting`)

- Symbols: $K$ frozen strata; $n_k$ paired units in stratum $k$; replicate index $b$; within-stratum draw $I_{bkj}\sim\mathrm{Uniform}\{1..n_k\}$.
- Units: mean unitless; reported in percentage points.
- Analysis unit: whole schedule-indexed paired-unit identifiers reweighted within strata; arms never reweighted separately.
- **Explicit equal-weighting statement (required, present in main.tex):** because strata may contain unequal $n_k$, the outer $1/K$ makes each replicate an **equally weighted average of stratum means**, not an unqualified battery mean; with equal $n_k$ the two coincide.
- Assumptions: the frozen within-stratum empirical distribution is the resampling object; conditional independence of draws within fixed strata.
- Toy calculation: one stratum, differences $[1,0,-1]$, sampled indices $[1,1,2]$ → $(1+1+0)/3=2/3$ (PASS today: 0.6667).
- Sign/index direction: inherits EQ02 sign convention per stratum; stratum means averaged with weight $1/K$ each.
- Source implementation: prespecified stratified resampling (100,000 draws, frozen analysis seed), verified independently from raw rows by `independent_statistics_audit.py`.
- Independent test: `tests/test_independent_statistics.py` (346 lines) + today's `--check` rerun (0 conflicts).
- Naming rule enforced: every displayed range is an **empirical reweighting quantile / descriptive sensitivity range**. No manuscript location calls these confidence intervals, hypothesis tests, or population intervals.

## EQ04 — Two-by-two factorial contrasts (Appendix `eq:factorial`)

- Symbols: cell win rates $\mu_{00},\mu_{10},\mu_{01},\mu_{11}$ (representation × training subscripts); contrasts $T,R,G,J$ as defined in Appendix B.
- Units: unitless before ×100 presentation.
- Analysis unit: same paired-unit definition as EQ02 within each cell.
- Assumptions: fixed packages; contrasts are algebraic identities, not mechanism-isolating effects unless design assumptions hold.
- Toy calculation: rates $(0.50,0.52,0.51,0.54)$ → $(T,R,G,J)=(0.04,0.025,0.015,0.01)$ (PASS today).
- Sign/index direction: first subscript representation, second training; $J$ is the interaction $\mu_{11}-\mu_{10}-\mu_{01}+\mu_{00}$.
- Source implementation: factorial contrast computation in the analyzer; macros generated by `build_final_artifacts.py`.
- Independent test: sign/direction tests in `tests/test_equations.py`; raw-row recomputation in `independent_statistics_audit.py`.

## Cross-cutting checks

| Check | Result |
|---|---|
| Every displayed empirical range labeled descriptive | PASS (grep + contradiction audit; no CI/hypo-test language attached to quantiles) |
| McNemar value excluded from admitted inference | PASS (retained only as numerical audit; stated in Methods/D01) |
| No inference drawn from nonsignificance/equivalence | PASS (contradiction audit scan) |
| Sign/index consistency across text, tables, figures | PASS (build report + figure source data regenerated deterministically) |
| Human comprehension signoff | PENDING (human-only) |
