# Clean-environment transitive dependency snapshot (text form)
#
# Source of record: paper/final_protocol/CLEAN_ENV_REPRODUCTION.json (status PASS, recorded 2026-08-25).
# This file is a convenience text copy for humans; the JSON record is authoritative.
#
# Rebuild commands (from the article repository root):
#   python3 -m venv .venv-clean
#   .venv-clean/bin/pip install numpy==2.4.6 matplotlib==3.10.5 pytest==9.1.1
#   .venv-clean/bin/python -B -m pytest -q -p no:cacheprovider   (cwd: paper/final_protocol/release)
#   .venv-clean/bin/python -B -m pevl_bench generate             (cwd: paper/final_protocol/release)
#   .venv-clean/bin/python -B -m pevl_bench verify               (cwd: paper/final_protocol/release)
#   .venv-clean/bin/python -B scripts/verify_release.py          (cwd: paper/final_protocol/release)
#
# Direct packages:
numpy==2.4.6
matplotlib==3.10.5
pytest==9.1.1

# Observed transitive freeze (pip freeze of the clean environment):
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
iniconfig==2.3.0
kiwisolver==1.5.0
matplotlib==3.10.5
numpy==2.4.6
packaging==26.3
pillow==12.3.0
pluggy==1.6.0
Pygments==2.21.0
pyparsing==3.3.2
pytest==9.1.1
python-dateutil==2.9.0.post0
six==1.17.0

# Platform scope: macOS arm64, CPython 3.11.5. Universal cross-platform
# portability is not claimed.
