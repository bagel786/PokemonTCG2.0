# Secrets and personal-data scan

Scope: files under `independent_confirmation/`.

Automated scan categories: API-key labels, secret/password labels, private-key labels, access-token labels, bearer-token patterns, GitHub token prefixes, and common `sk-` token forms.

Result: no matching credential pattern found at package validation time.

The stop package contains repository identifiers, local filesystem paths, cited author names, and the pre-existing Git identity facts necessary for audit. No raw participant data, author address, affiliation, ORCID, signature, reviewer identity, institutional approval, or journal credential was added. This automated scan is not a substitute for a human privacy and rights review, and no public release is authorized.
